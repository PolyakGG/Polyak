package main

import (
	"fmt"
	"strings"
)

// Используя тип rune, вы можете представлять отдельные символы в строке. В моем предыдущем ответе, когда я использовал char,
//  это было сокращением для переменной, которая содержит символ типа rune.
// код for _, char := range str перебирает каждый символ строки str и сохраняет его в переменной char, которая является типом rune.
//  в Go, тип rune используется для представления символов Юникода, и он часто используется при работе с строками.
func main() {
	var stroka string
	fmt.Scan(&stroka) // Input slova
	var result string
	for _, char := range stroka { // char = unicode symbols // for _, char := range str: Мы начинаем цикл for,  который перебирает каждый символ (char) в исходной строке str.
		if strings.Count(stroka, string(char)) == 1 { //Здесь мы используем функцию strings.Count для подсчета количества вхождений символа char в строку str.
			result += string(char) //  Мы сравниваем результат с 1, чтобы определить, сколько раз данный символ встречается в строке.
		} //Если символ char встречается только один раз в строке str, то мы добавляем его к строке result.
	}
	fmt.Print(result)

}
